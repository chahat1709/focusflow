[check]
IPv6 = 0 # Disable IPv6 to prevent lookup delays

[multicast]
# Force local interface
Interfaces = 127.0.0.1 
TTL = 0 # Local only

[lab]
# Optimize for local testing
SessionID = default
KnownPeers = 127.0.0.1
